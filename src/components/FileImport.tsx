import React from "react"
import { MatrixType } from "../types/Matrix"

const FileImport = ({ size, type, matrixValues, vectorValues } : { size: number, type: MatrixType, matrixValues: number[][], vectorValues: number[] }) => void {
    
}